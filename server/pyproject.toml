[project]
name = "qwen-ane-llm-server"
version = "0.1.0"
description = "OpenAI compatible server for qwen-ane-llm"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "httpx",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
